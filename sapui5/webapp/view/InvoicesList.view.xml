<mvc:View controllerName="logaligroup.sapui5.controller.InvoicesList"
            xmlns="sap.m"
            xmlns:mvc="sap.ui.core.mvc">
    <Panel accessibleRole="Region">
    <headerToolbar>
            <Toolbar >
                <Title text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer />
                <SearchField width="50%" search=".onFilterInvoices"
                    placeholder="{i18n>searchPFieldHolder}"></SearchField>
            </Toolbar>
        </headerToolbar>
    <Table 
        id="InvoiceList"
        headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" 
        items="{path: 'northwind>/Invoices' ,
                sorter : {path : 'ShipperName',
                            group : true}
                }">
        
        <columns>
            <Column hAlign="End" minScreenWidth="Small" demandPopin="true" width="4em">
                <Text text="{i18n>columQuantity}"></Text>
            </Column>
            <Column >
                <Text text="{i18n>columName}"></Text>
            </Column>
            <Column minScreenWidth="Small" demandPopin="true">
                <Text text="{i18n>columStatus}"></Text>
            </Column>
            <Column minScreenWidth="Tablet">
                <Text text="{i18n>columSupplier}"></Text>
            </Column>
            <Column hAlign="End">
                <Text text="{i18n>columPrice}"></Text>
            </Column>
        </columns>
        <items>
            <ColumnListItem type="Navigation" press="navigateToDetails">
                <cells>
                    <ObjectNumber number="{northwind>Quantity}" emphasized="false">
                    </ObjectNumber>
                    <ObjectIdentifier title="{northwind>ProductName}"></ObjectIdentifier>
                    <Text text="{
                        path: 'northwind>Status',
                        formatter: '.formatter.invoiceStatus'
                    }"></Text>
                    <Text text="{northwind>ShipperName}"></Text>
                    <ObjectNumber number="{ parts : [{path: 'northwind>ExtendedPrice'},
                                                {path: 'currency>/usd'}],
                                    type: 'sap.ui.model.type.Currency',
                                    formatOptions:{
                                        showMeasure: false
                                    }}"
                            unit="{currency>/usd}"
                            state="{= ${northwind>ExtendedPrice} > 50 ? 'Success' : 'Error'}"></ObjectNumber>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
    </Panel>

</mvc:View>